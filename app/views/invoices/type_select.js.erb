var idx = <%= @idx %>;
$("#invoice_billingdetails_attributes_" + idx + "_product_id").html("<%= escape_javascript(options_for_select(@products)) %>");

$("#invoice_billingdetails_attributes_" + idx + "_dummy").html("<%= escape_javascript(options_for_select(@prices)) %>");
